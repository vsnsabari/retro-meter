import { createStyles, Theme } from "@material-ui/core";

export const commentsStyles = (theme: Theme) => createStyles({
    gridContainer: {
        display: 'flex',
        paddingRight: 0,
        paddingLeft: 0,
        paddingTop: 10
    }
});